# `scripts/`

Developer and CI shell scripts: local bootstrap, release helpers, schema regeneration, lint shortcuts, and anything else that is easier as a script than a Makefile target.

Scripts here should be POSIX-portable where reasonable (`#!/usr/bin/env bash`, `set -euo pipefail`) and document their inputs at the top.
