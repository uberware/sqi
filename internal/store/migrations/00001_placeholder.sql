-- +goose Up
-- Placeholder migration. The full schema (farms, queues, jobs, steps, tasks,
-- workers, sessions, storage_locations, license_pools, etc.) is implemented
-- in tasks 25–27.

-- +goose Down
