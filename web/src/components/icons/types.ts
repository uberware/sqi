// SPDX-License-Identifier: AGPL-3.0-or-later

/** Standard props every icon accepts. */
export interface IconProps {
  /** Square edge length in px, applied as width/height. Defaults to 16. */
  size?: number | undefined
  /** Extra class names, e.g. for color or animation overrides. */
  className?: string | undefined
}
